import { View } from "@tarojs/components";
import styles from "./styles.module.scss";

const QuestionDetail = () => {
  return <View className={styles.wrapper}>detail</View>;
};

export default QuestionDetail;
